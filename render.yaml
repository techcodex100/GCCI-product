build:
  environment:
    PYTHON_VERSION: 3.11.9  # Force specific version
    PIP_VERSION: 23.3.2      # Match your local environment
services:
  - type: web
    name: GCCI
    runtime: python          # Explicitly specify runtime